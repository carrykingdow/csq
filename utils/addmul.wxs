var filters = {
  toFix: function(value) {
    return value.toFixed(2) //此处2为保留两位小数
  },
  toInt: function (value) {
    return parseInt(value)
  }
}
module.exports = {
  toFix: filters.toFix,
  toInt: filters.toInt
}